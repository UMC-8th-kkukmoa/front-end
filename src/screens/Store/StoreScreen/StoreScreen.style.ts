import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    position: 'relative',
  },

  mapArea: {
    flex: 1,
  },

  headerArea: {
    position: 'absolute',
    top: 2,
  },
});

export default styles;

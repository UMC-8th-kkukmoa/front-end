import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    position: 'relative',
  },

  mapArea: {
    flex: 1,
  },

  headerArea: {
    position: 'absolute',
    paddingTop: 50,
  },
});

export default styles;
